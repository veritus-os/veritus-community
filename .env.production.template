# Veritus OS — Production Configuration (Student Checkout)
# Copy to .env.production or .env.local before deploying.
#
# IMPORTANT: Never commit this file with real keys.
# The .env.* pattern is already in .gitignore.

# Supabase connection (required)
VITE_SUPABASE_URL=https://bnggrjkllpgisdgmnjvm.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-key>

# Low-egress mode (recommended for free tier)
VITE_CHECKOUT_SUPABASE_LOW_EGRESS=true

# Do NOT enable local mode in production
# VITE_CHECKOUT_LOCAL_MODE=false
