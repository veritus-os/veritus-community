# VeritusOS Community — environment template
# Copy to .env.local and fill in the values for your deployment.
# Never commit real secrets. .env and .env.* are gitignored (except this template).

# ─────────────────────────────────────────────────────────────
# Supabase mode (cloud / hosted backend)
# ─────────────────────────────────────────────────────────────
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# ─────────────────────────────────────────────────────────────
# Local physical server mode (Windows server @ Colégio Alta Vista)
# Serves Checkout/Search from the on-prem local API instead of Supabase.
#
# Single source of truth for the checkout API URL: .env.local.
# By DEFAULT the frontend derives the API host from the address used to open
# the app (e.g. the LAN IP) and calls <that-host>:3333 — so NO server IP is
# baked into the build. Leave VITE_LOCAL_API_URL unset for this (recommended).
# ─────────────────────────────────────────────────────────────
VITE_CHECKOUT_LOCAL_MODE=true
VITE_CHECKOUT_SUPABASE_LOW_EGRESS=false

# Optional override — set ONLY if the API runs on a different host/port than
# the frontend. A hardcoded host is per-site and fragile (breaks if DHCP
# changes the IP); prefer leaving it unset so browser-host derivation covers
# every machine automatically.
# VITE_LOCAL_API_URL=http://<SERVER_IP>:3333
